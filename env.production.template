# ===========================================
# PRODUCTION ENVIRONMENT TEMPLATE
# ===========================================
# This template shows what production values should look like
# In production, these are managed by AWS Secrets Manager

# ===========================================
# DJANGO SETTINGS (Production)
# ===========================================
SECRET_KEY=super-secure-secret-key-generated-by-aws-secrets-manager
DEBUG=False
ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com

# ===========================================
# DATABASE SETTINGS (Production)
# ===========================================
DB_NAME=neondb
DB_USER=neondb_owner
DB_PASSWORD=your-secure-neon-password
DB_HOST=ep-long-meadow-afa50phs-pooler.c-2.us-west-2.aws.neon.tech
DB_PORT=5432

# ===========================================
# CORS SETTINGS (Production)
# ===========================================
CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# ===========================================
# SECURITY SETTINGS (Production)
# ===========================================
SECURE_SSL_REDIRECT=True
SECURE_BROWSER_XSS_FILTER=True
SECURE_CONTENT_TYPE_NOSNIFF=True
X_FRAME_OPTIONS=DENY
SECURE_HSTS_SECONDS=31536000
SECURE_HSTS_INCLUDE_SUBDOMAINS=True
SECURE_HSTS_PRELOAD=True

# ===========================================
# FRONTEND SETTINGS (Production)
# ===========================================
REACT_APP_API_URL=https://yourdomain.com/api
REACT_APP_ENVIRONMENT=production

# ===========================================
# AWS SETTINGS (Production)
# ===========================================
AWS_ACCOUNT_ID=123456789012
AWS_REGION=us-east-1
ECR_REPOSITORY_NAME=codingal-ai-coach
ECS_CLUSTER_NAME=codingal-cluster
ECS_SERVICE_NAME=codingal-service

# ===========================================
# DOMAIN SETTINGS (Production)
# ===========================================
DOMAIN_NAME=yourdomain.com
PRODUCTION_URL=https://yourdomain.com
